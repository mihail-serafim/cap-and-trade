The cap_and_trade folder is placed into the www/ folder in wamp (for example). Then the scripts are accessible at localhost/cap_and_trade/<script-name>

cap_and_trade.sql is used to import into phpMyAdmin. You will also have to add a user with the same username and password found in secrets.ini (Privileges > add user account).